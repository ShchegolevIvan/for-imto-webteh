NewsApp — Frontend

React + Vite

 Описание

Фронтенд для новостного сервиса.
Позволяет просматривать список новостей, детали новости, входить в систему и создавать новости (если пользователь авторизован).

Фронт полностью отделён от backend-части и общается с API через HTTP-запросы.

 Запуск проекта
1. Установка зависимостей
npm install

2. Запуск в режиме разработки
npm run dev


После запуска приложение будет доступно по адресу:
 http://localhost:5173/

 Переменные окружения (если нужны)
Если понадобится подключить переменные окружения (например, URL API), создайте файл:
/.env
и добавьте:
VITE_API_URL=http://localhost:8000
📁 Структура проекта
lab6_Schegolev_I_O/
│── public/
│── src/
│   ├── pages/              # Страницы (Новости, Вход, Создание новости)
│   ├── components/         # Компоненты (Navbar)
│   ├── contexts/           # Контекст авторизации
│   ├── api/                # Axios-настройки
│   ├── App.jsx
│   └── main.jsx
│── package.json
│── vite.config.js
│── README.md
│── .gitignore

 Используемое API
Фронтенд рассчитывает, что backend работает на:
http://localhost:8000
И предоставляет следующие эндпоинты:
/news — список новостей
/news/{id} — детальная информация
/auth/login — авторизация
/auth/register — регистрация
(Можно менять в axios, если API у преподавателя другое.)

    Возможности
Просмотр списка новостей
Просмотр выбранной новости
Авторизация пользователя
Создание новостей после входа
Навигация между страницами

Сборка проекта
npm run build

После сборки файлы появятся в /dist.

 Требования
Node.js 18+
npm